/// @description 
platformSpeed = 1;
currentSpeed = 1;
moveX = 0;
moveY = 0;

goingToStart = false;
waitTime = 60;

// กำหนดค่าเริ่มต้นให้ตำแหน่งของแพลตฟอร์ม
startX = x;
startY = y;
endX = x + 100; // หรือกำหนดค่าที่เหมาะสม
endY = y;
